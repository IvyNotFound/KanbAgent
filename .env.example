# GitHub token for build-time embedding
# Build with: GITHUB_TOKEN=ghp_xxx npm run build
# Without this token, GitHub-authenticated features degrade gracefully (public repos still work)
GITHUB_TOKEN=
